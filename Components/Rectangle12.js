import React from "react"
import { StyleSheet, View, Text, Image } from "react-native"

export default function Rectangle12() {
  return <View style={styles.Rectangle12} />
}

const styles = StyleSheet.create({
  Rectangle12: {
    backgroundColor: "rgba(255,205,205,1)",
    transform: "rotate(-145.28deg);",
    width: 51,
    height: 24.19,
    borderRadius: 12,
    opacity: 0.5,
  },
})